import { COMMENTS } from '../shared/comments';


export  const Comments = (state= COMMENTS, action) => {  //takes state and actions as 2 parameters
    switch(action.type) {
        default:
            return state;
    }
}