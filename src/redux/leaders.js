import { LEADERS } from '../shared/leaders';

export  const Leaders = (state= LEADERS, action) => {  //takes state and actions as 2 parameters
    switch(action.type) {
        default:
            return state;
    }
}