import { PROMOTIONS } from '../shared/promotions';


export  const Promotions = (state= PROMOTIONS, action) => {  //takes state and actions as 2 parameters
    switch(action.type) {
        default:
            return state;
    }
}